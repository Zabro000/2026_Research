#include <stdio.h>
#include <string.h>

int winner(double t);

int main(int argc, char *argv[]){
	char buf[256];
	strcpy(buf, argv[1]);
	printf("%s\n", buf);
	return 0;
}


int winner(double t)
{
	double data = 90;
	printf("Successful attack");
}